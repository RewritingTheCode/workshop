name: Deploy

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: deploy-main
  cancel-in-progress: false

jobs:
  # The gate. This is the whole point: nothing below can start until lint,
  # typecheck, tests and build have all passed.
  ci:
    name: CI
    uses: ./.github/workflows/ci.yml

  # Secrets are not available to a job-level `if`, so we resolve them here and
  # hand the answer down. Without this, every clone of this template that has
  # not set up Netlify yet would show a red X on every push.
  secrets-present:
    name: Check Netlify secrets
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      ready: ${{ steps.check.outputs.ready }}
    steps:
      - id: check
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: |
          if [ -n "$NETLIFY_AUTH_TOKEN" ] && [ -n "$NETLIFY_SITE_ID" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            {
              echo "### Netlify deploy skipped"
              echo ""
              echo "Add \`NETLIFY_AUTH_TOKEN\` and \`NETLIFY_SITE_ID\` under"
              echo "Settings -> Secrets and variables -> Actions to deploy on every push."
              echo ""
              echo "Until then, deploy from your machine with \`npm run deploy\`."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

  deploy:
    name: Deploy to Netlify
    needs: [ci, secrets-present]
    if: needs.secrets-present.outputs.ready == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: production
      url: ${{ steps.netlify.outputs.url }}

    steps:
      # Pinned to a SHA for the same reason as in ci.yml, and it matters more
      # here: this is the job that holds the deploy token.
      - name: Check out the repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm

      # No dependency lifecycle script runs in the job that has the token.
      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build
        run: npm run build

      - name: Deploy to Netlify production
        id: netlify
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: |
          # Pinned, not @latest. This is the one step in the repo that holds a
          # credential, and `@latest` means "whatever the registry serves at
          # this exact second" executes with the deploy token in its
          # environment. Dependabot keeps the pin moving. See ADR-007.
          npx --yes netlify-cli@^27 deploy --dir=dist --prod --json > deploy.json
          url="$(node -p "require('./deploy.json').deploy_ssl_url || require('./deploy.json').url")"
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "Deployed to $url" >> "$GITHUB_STEP_SUMMARY"
